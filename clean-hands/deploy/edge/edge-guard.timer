# /etc/systemd/system/edge-guard.timer
# Poll every 60s. Enable with:  systemctl enable --now edge-guard.timer
[Unit]
Description=run edge-guard every 60s

[Timer]
OnBootSec=60
OnUnitActiveSec=60
AccuracySec=10s

[Install]
WantedBy=timers.target
