# CLEAN soft-staking API deps.
#   pip install -r requirements.txt
fastapi>=0.138,<1.0
starlette>=1.3.1,<2.0
uvicorn>=0.49,<1.0
httpx>=0.27,<1.0
PyNaCl>=1.5,<2.0     # Solana ed25519 signature verification
base58>=2.1,<3.0
eval-type-backport>=0.4,<1.0  # keeps Pydantic union annotations working on older Python 3.x
# Optional, only needed when you scale horizontally:
redis>=5.0,<6.0          # shared nonce + rate-limit store (set REDIS_URL)
# psycopg[binary]>=3.1,<4.0   # Postgres backend (set DATABASE_URL); uncomment to use
